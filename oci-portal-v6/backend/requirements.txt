# OCI Portal - Backend Dependencies
# Python 3.13+  (no Node.js required — frontend served by FastAPI)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Jinja2 templating (serves HTML pages)
jinja2==3.1.4
python-multipart==0.0.9

# OCI SDK
oci==2.126.0

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database ORM
sqlalchemy==2.0.35
aiosqlite==0.20.0            # async SQLite (dev)
psycopg[binary]==3.3.0       # async Postgres (prod) — psycopg3, already installed

# Config
python-dotenv==1.0.1
pydantic-settings==2.5.2
pydantic[email]==2.9.2

# Dev / testing
pytest==8.3.0
pytest-asyncio==0.24.0
httpx==0.27.2
